# INF-1400 Submission 2

Reserved for INF-1400 submission 2. This repository sometimes stores submission artifacts in nested subfolders rather than directly in the `submission-2/` folder.

If you are looking for code submitted for INF-1400, try a recursive search (e.g., `git ls-files` or GitHub search within the repository) or inspect student-specific subfolders.
